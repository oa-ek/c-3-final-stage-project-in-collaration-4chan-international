<Window x:Class="Program.MenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Soulslike Builder - Menu" Height="600" Width="800"
        Background="#080808" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="SoulsGold" Color="#C0A070"/>

        <Style TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <ScrollContentPresenter />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/images/backGroundMajulajpg.jpg" Stretch="UniformToFill" Opacity="1"/>
        </Grid.Background>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="LOAD GAME" Foreground="{StaticResource SoulsGold}" 
                       FontSize="30" FontFamily="Georgia" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10"/>
                </TextBlock.Effect>
            </TextBlock>

            <ListBox Grid.Row="1" x:Name="BuildsList" 
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="OnBuildDoubleClick"
                     Margin="0,0,0,20">

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Border Width="150" Height="150" BorderBrush="#555" BorderThickness="1" Background="#AA121212" Margin="5" CornerRadius="3">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="⚔️" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#333"/>

                                    <StackPanel Grid.Row="1" Margin="10">
                                        <TextBlock Text="{Binding Name}" Foreground="{StaticResource SoulsGold}" 
                                   FontSize="16" FontWeight="Bold" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Id, StringFormat='ID: {0}'}" Foreground="Gray" FontSize="12"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <Button Content="❌" 
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,5,5,0"
                    Width="25" Height="25"
                    Background="Transparent" BorderThickness="0" 
                    Foreground="IndianRed" FontWeight="Bold" Cursor="Hand"
                    Click="OnDeleteClick">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                                <Setter Property="Background" Value="#33000000"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                                        <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" TargetName="Bd" Value="#44333333"/>
                                            <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource SoulsGold}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource SoulsGold}"/>
                                            <Setter Property="BorderThickness" TargetName="Bd" Value="2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <StackPanel Grid.Row="2" HorizontalAlignment="Center" Width="300">
                <Button Content="NEW GAME" Click="NewCharacter_Click" 
                        Height="40" Margin="0,0,0,10"
                        Background="#222" Foreground="{StaticResource SoulsGold}" BorderBrush="#555" 
                        FontSize="14" FontFamily="Georgia" FontWeight="Bold"/>

                <Button Content="EXIT" Click="Exit_Click" 
                        Height="40"
                        Background="#151515" Foreground="#AAA" BorderBrush="#444" 
                        FontSize="14" FontFamily="Georgia"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>